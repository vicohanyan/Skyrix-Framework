// Package engine groups the core infrastructure services (database, cache, jobs).
// These components are wired by providers and used across handlers, middleware,
// and domain services.
package engine
